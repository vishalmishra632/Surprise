node_modules
dist
.env
.env.local
.DS_Store
*.tsbuildinfo
.vercel
.cache

# Legacy duplicate media folders — the active app reads src/assets/memories/.
# These were left over from a previous iteration; keep them on disk locally
# but don't ship them into the repo or deploy.
/images/
/memories/
/public/media/

.vercel

# scripts/render-reel.mjs scratch dir for the headless-Chromium WebM
# before it's encoded into public/reel.mp4. Don't ship the raw video.
/tmp/
